const turun = () => {
       for (i = 10; i >= 1; i -= 1){
              hasilPosisi = i - 1
              document.write(`<br>posisi ${i} turun satu posisi ${hasilPosisi}`)
       }
}
const naik = () => {
       for (i = 1; i <= 1; i += 1){
              hasilPosisi = i - 1
              document.write(`<br>posisi ${i} turun satu posisi ${hasilPosisi}`)
       }
}